apiVersion: v1
kind: Service
metadata:
  name: cluster-mcp
  namespace: cluster-mcp
  labels:
    app: cluster-mcp
spec:
  type: ClusterIP
  selector:
    app: cluster-mcp
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP