apiVersion: v1
kind: Service
metadata:
  name: presentation-frontend
  namespace: astdev-presentation
  labels:
    app: presentation-frontend
spec:
  type: NodePort
  selector:
    app: presentation-frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30000
    protocol: TCP
    name: http
